<h1 class='ember-pdf-loading' style='text-align: center;'>
  {{#if template}}
    {{yield}}
  {{else}}

    {{#if message}}
      {{message}}
    {{else}}
      {{defaultMessage}}
    {{/if}}
    
  {{/if}}
</h1>
